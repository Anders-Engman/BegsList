{{!-- TODO: create an element that shows either a welcome message to the logged in user, or a prompt to login or sign
up --}}
<div class="layout__inline-user">
    {{#if user}}
    Hello, {{user.name}}
    {{else}}
    <a data-toggle="modal" data-target="#signUpModal" class="layout__signup-modal">Sign-up</a> / <a data-toggle="modal"
        data-target="#loginModal" class="layout__login-modal">Log-in</a>
    {{/if}}
</div>